---
title: "Image Trail"
description: "Cursor trail effect that spawns images along it's path."
tags: ["Cursor Effect", "Cursor Trail", "Motion"]
---

<DocComponentPreview name="image-trail-demo" />

## Installation

<DocInstallGuide name="image-trail" />

---

## Usage
```tsx title="Imports"
import { ImageTrail } from "@/components/ImageTrail"
```

```tsx title="Usage example"
const PHOTOS = ["/img/1.jpg", "/img/2.jpg", "/img/3.jpg"]

<ImageTrail
    data={PHOTOS}
    renderItem={(src) => (
        <img src={src} className="w-20 h-20 rounded-lg object-cover" />
    )}
/>
```

```tsx title="Scoped to a container"
const PHOTOS = ["/img/1.jpg", "/img/2.jpg", "/img/3.jpg"]
const containerRef = useRef<ComponentRef<"div">>(null)

<div ref={containerRef} className="w-100 h-100">
    <ImageTrail
        data={PHOTOS}
        renderItem={(src) => (
            <img src={src} className="w-20 h-20 rounded-lg object-cover" />
        )}
    />
</div>
```

---

## API

| Name            | Type                        | Default | Description                                              |
| --------------- | --------------------------- | ------- | -------------------------------------------------------- |
| `data`          | `T[]`                       | —       | Array of items.|
| `renderItem`    | `(item: T) => ReactNode`    | —       | Render function called for each items.|
| `spawnDistance`  | `number`                    | `120`   | Distance in pixels before spawning an item.|
| `driftAmount`   | `number`                    | `60`    | How far each item drifts from its spawn point in px.|
| `removeDelay`   | `number`                    | `1.2`   | Seconds before a spawned item is removed.|

---

## Notes

- **touch-action**: Use `touch-action: none` to avoid scroll interference on mobile.
- **spawnDistance**: Lower `spawnDistance` for mobile to account for shorter drag distances.

---

## Credits

[Motion](https://motion.dev)  
React animation library.
