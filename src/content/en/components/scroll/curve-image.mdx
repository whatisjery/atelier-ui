---
title: "Curve Image"
description: "An SEO-friendly WebGL image with scroll-velocity driven bend and chromatic aberration."
tags: ["React Three Fiber", "Drei", "Lenis", "Tailwind CSS"]
---

import Alert from "@/components/ui/Alert";
import { Box } from "lucide-react";

<DemoPreview
  name="curve-image"
  controls={{
    amplitude: { type: "slider", value: 0.03, min: 0, max: 0.15, step: 0.01 },
    aberration: { type: "slider", value: 0.003, min: 0, max: 0.02, step: 0.001 },
    smoothing: { type: "slider", value: 6, min: 1, max: 20, step: 0.5 },
    segments: { type: "slider", value: 32, min: 4, max: 64, step: 4 },
    webglEnabled: { type: "boolean", value: true },
  }}
/>

---

<Alert icon={<Box strokeWidth={1.5} className="size-6" />} title="Prerequisite">
  This component renders into a shared React Three Fiber Canvas via the
  AtelierRig. Follow the [WebGL
  setup](/docs/getting-started/installation#set-up-webgl) once and every
  component will work.
</Alert>

---

## CLI Install

<InstalGuideCLI name="curve-image" />

---

## Manual Install

<InstalGuideManual name="curve-image" />

---

## API

| Name         | Type                | Default | Description                                                                     |
| ------------ | ------------------- | ------- | ------------------------------------------------------------------------------- |
| src          | `string`            | —       | Image URL bound to the GPU plane.                                               |
| alt          | `string`            | —       | Alt text for the underlying `<img>`.                                            |
| amplitude    | `number`            | `0.03`  | Maximum bend intensity, applied to the smoothed scroll velocity.                |
| aberration   | `number`            | `0.003` | RGB channel split along the scroll axis, scaled by velocity.                    |
| smoothing    | `number`            | `6`     | Exponential smoothing for the velocity (higher = snappier, lower = floatier).   |
| segments     | `number`            | `32`    | Plane subdivision count. Higher = smoother curve, more vertices.                |
| webglEnabled | `boolean`           | `true`  | When false, the WebGL plane is skipped and the plain `<img>` is rendered as-is. |
| ...props     | `ImgHTMLAttributes` | —       | Other `<img>` attributes forwarded to the underlying element.                   |

---

## Credits

[React Three Fiber](https://r3f.docs.pmnd.rs/)  
React renderer for Three.js used for the WebGL plane.

[Lenis](https://www.lenis.dev/)  
Smooth scroll library used in the demo.
