---
title: Installation
description: How to start using Atelier UI, not much.
icon: "FolderDown"
order: 1
tags: ["Tailwind CSS", "Copy & paste", "Ready in minutes"]
---

import DocStep from "@/components/features/docs/DocStep";

<ProLicenseHelper />

<DocStep step={1}>

## Set up Tailwind CSS

This library is built on Tailwind CSS v4. If your project doesn't already use it, follow the Tailwind CSS [installation guide](https://tailwindcss.com/docs/installation) before continuing.

</DocStep>

<DocStep step={2}>

## Set up React Three Fiber

You only need one `<Canvas>` for the whole app. Place it at your app's root with `<WebglPortal />` inside. It connects components like [CurveImage](/docs/components/scroll/curve-image) to the Canvas so they can render their effects from anywhere in your React tree.

<InstalGuideCLI name="webgl-portal" />

```tsx title="Root layout"
import { Canvas } from "@react-three/fiber";
import { WebglPortal } from "./webgl-portal";

// The canvas must cover the full viewport
<Canvas style={{ position: "fixed", inset: 0, pointerEvents: "none" }}>
  {/* required for components used elsewhere in your tree */}
  <WebglPortal />

  {/* components that live inside the Canvas go here */}
</Canvas>;
```

</DocStep>

<DocStep step={3}>

## Set up pro license

**Pro components** require a license. Copy your key from the panel at the top of this page and add it to `.env.local`.

```text title=".env.local"
ATELIER_PRO_KEY=<your-license-key>
```

Restart your dev server after adding the key.

</DocStep>

<DocStep step={4}>

## Install a component

Pick a component from the docs, then install it:

- **CLI** — installs the source and any shared dependencies.
- **Manual** — copy each file by hand from the component page.

```text title="CLI example"
npx atelier-ui add fluid-distortion
```

</DocStep>
